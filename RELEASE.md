# 🚀 发布指南

本项目使用 GitHub Actions 自动化发布流程，包括创建 GitHub Release 和发布到 pkg.go.dev。

## 📋 自动化发布流程

### 1. 创建版本标签

发布新版本只需推送一个版本标签：

```bash
# 创建版本标签
git tag -a v0.2.0 -m "Release v0.2.0: Add new features"

# 推送标签到 GitHub
git push origin v0.2.0
```

### 2. 自动化流程

推送标签后，GitHub Actions 会自动执行：

#### ✅ **Release Job** (步骤 1)
1. 检出代码
2. 设置 Go 环境
3. 运行测试套件
4. 构建验证
5. 生成变更日志
6. 创建 GitHub Release
   - 包含安装说明
   - 包含 pkg.go.dev 链接
   - 包含变更日志
   - 自动判断是否为预发布版本

#### ✅ **Publish to pkg.go.dev Job** (步骤 2)
1. 通过 Go Proxy 触发索引
2. 使用 `go get` 触发索引
3. 请求 pkg.go.dev API
4. 等待索引完成
5. 验证包可用性
6. 输出发布信息

### 3. 发布结果

完成后，你会得到：

- ✅ GitHub Release 页面（带详细说明）
- ✅ pkg.go.dev 上的包文档
- ✅ 可通过 `go get` 安装的版本

## 🏷️ 版本命名规范

遵循 [Semantic Versioning 2.0.0](https://semver.org/)：

### 正式版本
```bash
v1.0.0      # Major release
v0.2.0      # Minor release  
v0.1.1      # Patch release
```

### 预发布版本
```bash
v0.2.0-alpha.1    # Alpha 版本
v0.2.0-beta.1     # Beta 版本
v0.2.0-rc.1       # Release Candidate
```

预发布版本会自动在 GitHub Release 中标记为 "Pre-release"。

## 📝 准备发布

### 1. 更新代码
```bash
# 确保所有更改已提交
git status

# 推送到远程仓库
git push origin main
```

### 2. 创建变更日志（可选）

如果有 `CHANGELOG.md` 文件，Release 会自动提取对应版本的变更：

```markdown
## [0.2.0] - 2025-12-06

### Added
- 新功能 A
- 新功能 B

### Changed
- 改进了性能

### Fixed
- 修复了 bug #123
```

如果没有 CHANGELOG.md，会自动使用 git commit 历史。

### 3. 创建并推送标签

```bash
# 创建标签
git tag -a v0.2.0 -m "Release v0.2.0: Summary of changes"

# 推送标签（触发自动发布）
git push origin v0.2.0
```

### 4. 监控发布流程

访问 GitHub Actions 页面查看进度：
```
https://github.com/lihongjie0209/sm-go-bc/actions
```

## ✅ 验证发布

### 1. 检查 GitHub Release
```
https://github.com/lihongjie0209/sm-go-bc/releases
```

应该能看到：
- ✅ 发布标题和描述
- ✅ 安装说明
- ✅ 文档链接
- ✅ 变更日志

### 2. 检查 pkg.go.dev

访问包页面（通常需要等待 5-10 分钟）：
```
https://pkg.go.dev/github.com/lihongjie0209/sm-go-bc@v0.2.0
```

### 3. 测试安装

```bash
# 在新目录测试
mkdir test-install
cd test-install
go mod init test
go get github.com/lihongjie0209/sm-go-bc@v0.2.0
```

## 🔄 发布流程图

```
开发者推送标签
    ↓
GitHub Actions 触发
    ↓
┌─────────────────────────────────┐
│   Release Job                   │
│   - 运行测试                     │
│   - 构建验证                     │
│   - 创建 GitHub Release          │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│   Publish to pkg.go.dev Job     │
│   - 触发 Go Proxy 索引          │
│   - 请求 pkg.go.dev             │
│   - 验证可用性                   │
└─────────────────────────────────┘
    ↓
发布完成
```

## 🛠️ 手动发布（备用方案）

如果自动化流程失败，可以手动执行：

### 1. 创建 GitHub Release
```bash
gh release create v0.2.0 \
  --title "Release v0.2.0" \
  --notes "Release notes here"
```

### 2. 触发 pkg.go.dev 索引
```bash
# 方式 1: 使用 go get
GOPROXY=https://proxy.golang.org,direct \
  go get github.com/lihongjie0209/sm-go-bc@v0.2.0

# 方式 2: 访问 pkg.go.dev 页面并点击 Request 按钮
# https://pkg.go.dev/github.com/lihongjie0209/sm-go-bc@v0.2.0
```

## 🐛 故障排查

### 问题: GitHub Release 创建失败

**解决方案:**
1. 检查 GitHub Actions 日志
2. 确认测试全部通过
3. 确认标签格式正确 (v*.*.*)
4. 检查仓库权限设置

### 问题: pkg.go.dev 未显示新版本

**解决方案:**
1. 等待 5-30 分钟（索引需要时间）
2. 手动访问页面并点击 Request 按钮
3. 确认标签已推送：`git ls-remote --tags origin`
4. 验证模块可下载：`go list -m github.com/lihongjie0209/sm-go-bc@v0.2.0`

### 问题: 测试在 CI 中失败

**解决方案:**
1. 在本地运行完整测试：`go test -v -race ./...`
2. 修复失败的测试
3. 推送修复
4. 删除旧标签并重新创建

```bash
# 删除本地和远程标签
git tag -d v0.2.0
git push origin :refs/tags/v0.2.0

# 重新创建并推送
git tag -a v0.2.0 -m "Release v0.2.0"
git push origin v0.2.0
```

## 📊 发布检查清单

在推送标签前，确认：

- [ ] 所有测试通过：`go test ./...`
- [ ] 代码已格式化：`go fmt ./...`
- [ ] 静态检查通过：`go vet ./...`
- [ ] 更新了 CHANGELOG.md（可选）
- [ ] 更新了版本相关文档
- [ ] 所有更改已提交并推送
- [ ] 标签名称遵循 semver 规范

## 📚 相关资源

- [GitHub Actions 工作流](.github/workflows/release.yml)
- [Semantic Versioning](https://semver.org/)
- [Go Modules: Publishing](https://go.dev/doc/modules/publishing)
- [pkg.go.dev About](https://pkg.go.dev/about)

---

**Happy Releasing! 🎉**
